<%- title = t('page_titles.donated_devices.responsible_body.interest.device_types.title') %>
<%- content_for :title, title %>
<%- content_for :before_content do %>
  <% breadcrumbs([
                   { "Home" => responsible_body_home_path },
                   title
                 ]) %>
<% end %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">

    <%= form_for @form, url: responsible_body_donated_devices_what_devices_do_you_want_path(@school), method: :post do |f| %>
      <%= f.govuk_error_summary %>

      <%= f.govuk_collection_check_boxes :device_types,
                                         @form.available_device_types,
                                         :id,
                                         :name,
                                         legend: {size: 'xl', text: title },
                                         hint: {size: 'm', text: t('hint', scope: scope) } %>
      <%= f.govuk_submit 'Continue' %>
    <%- end %>
  </div>
</div>
