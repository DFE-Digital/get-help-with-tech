<%- content_for :title, t('page_titles.token_is_valid_but_expired') %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <h1 class="govuk-heading-xl">
      <%= t('page_titles.token_is_valid_but_expired') %>
    </h1>
    <p class="govuk-body-l">
      Sign-in links only work for <%= ActiveSupport::Duration.build(Settings.sign_in_token_ttl_seconds).inspect %>,
      so you need to request a new one. We'll send this by email.
    </p>
    <p class="govuk-body">
      <%= govuk_button_link_to 'Request a new sign-in link', sign_in_path %>
    </p>
  </div>
</div>
